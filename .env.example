# Application under test
BASE_URL=https://www.saucedemo.com

# Standard demo credentials (public test data for saucedemo.com)
APP_USERNAME=standard_user
APP_PASSWORD=secret_sauce

# Playwright run settings
HEADLESS=true
BROWSER=chromium

# AI provider used by apps/automation/utils/aiClient.ts
# Get a free key (no credit card required) at https://aistudio.google.com/apikey
GEMINI_API_KEY=AIzaSyBSLti4JEcWwtg3JQ4Og41Xt1WpCEBwJYY
AI_MODEL=gemini-2.5-flash

# Set to "true" to let AI-assertion tests soft-fail (log only) instead of
# failing the build when the AI flags something - useful while tuning prompts.
AI_ASSERTIONS_SOFT_FAIL=false
